// root = top most node. The parent
//height = the longest path from the root to the most far away child 
//binary three = a three with at max two children (left and right instead of a children)
//general three  = a three with 0 or more children 
//binary search tree = a tree in which has a specific ordering to the nodes and at most 2 children 
//leaves - a node without children 
//balanced = a three is perfectly balanced when any node's left and right children have the same height. 
//branching factor = the amount of children tree has. 